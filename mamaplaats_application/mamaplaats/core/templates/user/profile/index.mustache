<section class="blog-masonry following-blog-masonry">
	<header class="heading-box">
		<h1>Blogs van moeders die ik volg</h1>
	</header>
	<div class="grid-box">
		{{#blogs}}
			<article class="item">
				<figure class="photo">
					<a class="photo-wrap" href="/blog/{{blog.slug}}">
						<b><img alt="{{media.title}}" src="{{media.path}}" width="230" height="140"></b>
					</a>
				</figure>
				<h4><a href="/blog/{{blog.slug}}">{{blog.title}}</a></h4>
				<p class="info">
					<a href="/lid/{{user.slug}}">{{user.full_name}}</a> -
					<time datetime="{{blog.created}}">{{blog.created_formatted}}</time>
				</p>
				<p>{{blog.excerpt}}</p>
			</article>
		{{/blogs}}

		{{^following}}
			<div>
				Houd je favorite moeders in de gaten en zorg dat je ze volgt. De blogberichten van deze moeders worden dan hier weergegeven! Momenteel volg je nog geen moeder. <a href="{{basepath}}follow/suggestions">Klik hier om moeders te volgen</a>
			</div>
		{{/following}}
	</div>
</section>
<input type="hidden" value="3" id="limit-following">
{{#load_more_following_blogs}}
<a href="#" id="load-more-btn-following" onclick="return load_more.user_profile_dashboard('following', 6)" class="load-more">Klik hier om meer berichten te laden</a>
{{/load_more_following_blogs}}
<section class="blog-masonry most_read-blog-masonry">
    <header class="heading-box">
        <h1>Meest gelezen blogs</h1>
    </header>
	<div class="grid-box">
		{{#top_blogs}}
			<article class="item">
				<figure class="photo">
					<a class="photo-wrap" href="/blog/{{blog.slug}}">
						<b><img alt="{{media.title}}" src="{{media.path}}" width="230" height="140"></b>
					</a>
				</figure>
				<h4><a href="/blog/{{blog.slug}}">{{blog.title}}</a></h4>
				<p class="info">
					<a href="/lid/{{user.slug}}">{{user.full_name}}</a> -
					<time datetime="{{blog.created}}">{{blog.created_formatted}}</time>
				</p>
				<p>{{blog.excerpt}}</p>
			</article>
		{{/top_blogs}}
	</div>
	{{^top_blogs}}
        <div class="alert alert-info">
            Geen blog gevonden
        </div>
	{{/top_blogs}}
</section>
<input type="hidden" value="3" id="limit-most_read">
{{#load_more_top_blogs}}
<a href="#" id="load-more-btn-most_read" onclick="return load_more.user_profile_dashboard('most_read', 6)" class="load-more">Klik hier om meer berichten te laden</a>
{{/load_more_top_blogs}}
<section class="blog-masonry list-blog-masonry">
    <header class="heading-box">
        <h1>Mijn blogs</h1>
    </header>
    <div class="grid-box">
		{{#own_blogs}}
            <article class="item">
                <figure class="photo">
                    <a class="photo-wrap" href="/blog/{{blog.slug}}">
                        <b><img alt="{{media.title}}" src="{{media.path}}" width="230" height="140"></b>
                    </a>
                </figure>
                <h4><a href="/blog/{{blog.slug}}">{{blog.title}}</a></h4>
                <p class="info">
                    <a href="/lid/{{user.slug}}">{{user.full_name}}</a> -
                    <time datetime="{{blog.created}}">{{blog.created_formatted}}</time>
                </p>
                <p>{{blog.excerpt}}</p>
            </article>
		{{/own_blogs}}
    </div>
	{{^own_blogs}}
        <div class="alert alert-info">
            Geen blog gevonden
        </div>
	{{/own_blogs}}
</section>
<input type="hidden" value="3" id="limit-list">
{{#load_more_blogs}}
<a href="#" id="load-more-btn-list" onclick="return load_more.user_profile_dashboard('list', 6)" class="load-more">Klik hier om meer berichten te laden</a>
{{/load_more_blogs}}